# Isolated shared BRTI infrastructure service only.
[build]
builder = "railpack"

[deploy]
startCommand = "python -u brti_shared_feed_guarded_launcher_v1.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"
healthcheckTimeout = 120
